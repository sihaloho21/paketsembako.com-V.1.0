kind = "web"
previewPath = "/"
title = "Hypermart Store"
version = "1.0.0"
id = "artifacts/hypermart-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22716

[services.development]
run = "pnpm --filter @workspace/hypermart-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hypermart-store", "run", "build" ]
publicDir = "artifacts/hypermart-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22716"
BASE_PATH = "/"
